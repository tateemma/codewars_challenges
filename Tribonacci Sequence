'''Python
def tribonacci(signature, n):
    result = []
    for i in range(n):
        if i < 3:
            result.append(signature[i])
        elif i >= 3:
            trib_i = result[i-1] + result[i-2] + result[i-3]
            result.append(trib_i)
    return result
'''
